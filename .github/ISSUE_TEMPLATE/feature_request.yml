name: Feature request
description: Suggest a new feature or improvement
title: "[Feature]: "
labels: ["enhancement"]
body:
  - type: markdown
    attributes:
      value: |
        Thanks for the idea! A short conversation here saves everyone time before any code is written.

        ncode is intentionally focused — not every idea will make it in. A "no" is not a judgment of the idea.

  - type: textarea
    id: problem
    attributes:
      label: What problem does this solve?
      description: |
        The **why**, not the **what**. What were you trying to do when you wished this existed?
      placeholder: "When I'm working with multiple providers, I want to switch between them without restarting ncode."
    validations:
      required: true

  - type: textarea
    id: proposal
    attributes:
      label: Proposed solution
      description: How would you imagine using this? UX sketch, CLI flags, behavior, anything that helps us picture it.
    validations:
      required: true

  - type: textarea
    id: alternatives
    attributes:
      label: Alternatives considered
      description: What workarounds have you tried? What other tools handle this well?
      placeholder: "I tried X, but it doesn't solve Y because..."

  - type: dropdown
    id: contribution
    attributes:
      label: Are you willing to contribute the implementation?
      options:
        - "Yes, with guidance"
        - "Yes, I can do it"
        - "No, just suggesting"
    validations:
      required: true

  - type: checkboxes
    id: checks
    attributes:
      label: Before submitting
      options:
        - label: I searched existing issues and didn't find a duplicate
          required: true
